From 8daa87290ee7fa3717bcecd52f58db0eff4e9974 Mon Sep 17 00:00:00 2001
From: Alyssa Rosenzweig <alyssa@rosenzweig.io>
Date: Tue, 6 Nov 2018 19:28:01 -0800
Subject: [PATCH 081/151] Appease hanetzer's compiler

---
 src/gallium/drivers/panfrost/midgard/midgard_compile.c | 3 +++
 src/gallium/drivers/panfrost/pan_context.c             | 6 ++++++
 2 files changed, 9 insertions(+)

diff --git a/src/gallium/drivers/panfrost/midgard/midgard_compile.c b/src/gallium/drivers/panfrost/midgard/midgard_compile.c
index d228d36..8f36411 100644
--- a/src/gallium/drivers/panfrost/midgard/midgard_compile.c
+++ b/src/gallium/drivers/panfrost/midgard/midgard_compile.c
@@ -1475,6 +1475,7 @@ midgard_ra_select_callback(struct ra_graph *g, BITSET_WORD *regs, void *data)
         }
 
         assert(0);
+        return 0;
 }
 
 static bool
@@ -1729,6 +1730,7 @@ component_from_mask(unsigned mask)
         }
 
         assert(0);
+        return 0;
 }
 
 static bool
@@ -2236,6 +2238,7 @@ quadword_size(int tag)
 
         default:
                 assert(0);
+                return 0;
         }
 }
 
diff --git a/src/gallium/drivers/panfrost/pan_context.c b/src/gallium/drivers/panfrost/pan_context.c
index 8762c65..67c0c5a 100644
--- a/src/gallium/drivers/panfrost/pan_context.c
+++ b/src/gallium/drivers/panfrost/pan_context.c
@@ -635,6 +635,7 @@ panfrost_translate_texture_swizzle(enum pipe_swizzle s)
 
         default:
                 assert(0);
+                return 0;
         }
 }
 
@@ -656,6 +657,7 @@ translate_tex_wrap(enum pipe_tex_wrap w)
 
         default:
                 assert(0);
+                return 0;
         }
 }
 
@@ -671,6 +673,7 @@ translate_tex_filter(enum pipe_tex_filter f)
 
         default:
                 assert(0);
+                return 0;
         }
 }
 
@@ -709,6 +712,7 @@ panfrost_translate_compare_func(enum pipe_compare_func in)
                 return MALI_FUNC_ALWAYS;
         }
 
+        assert (0);
         return 0; /* Unreachable */
 }
 
@@ -741,6 +745,7 @@ panfrost_translate_alt_compare_func(enum pipe_compare_func in)
                 return MALI_ALT_FUNC_ALWAYS;
         }
 
+        assert (0);
         return 0; /* Unreachable */
 }
 
@@ -773,6 +778,7 @@ panfrost_translate_stencil_op(enum pipe_stencil_op in)
                 return MALI_STENCIL_INVERT;
         }
 
+        assert (0);
         return 0; /* Unreachable */
 }
 
-- 
2.7.4

